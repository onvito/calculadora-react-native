import React from 'react'
import { View, Text, SafeAreaView} from 'react-native'

export default props => {
    return (
       <SafeAreaView>
           <Text> Fala zé </Text>
       </SafeAreaView>
    )
}